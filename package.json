{
  "name": "aura-beauty-studio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && wrangler pages deploy dist --project-name aura-beauty-studio"
  },
  "dependencies": {
    "astro": "^6.1.2",
    "wrangler": "^4.79.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
